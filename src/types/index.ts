export type ChildrenProp = {
  children: React.ReactNode;
}

export type CardItem = {
  title: string;
  description?: string;
  list?: string[]
}

export type MenuItem = {
  title: string
  href: string
  description?: string
}